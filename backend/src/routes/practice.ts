import { Router } from "express";

const router = Router();

// Handled in sessions.ts as POST /sessions/analyze via /practice/analyze mount
// This file kept for extensibility

export default router;
